# Spring Boot Milestone 03: Request Mapping

A Spring Boot exercise that maps browser requests to controller methods and renders a response with a template.

## What I practised

- `@Controller` and request mappings
- Request parameters
- Thymeleaf templates

## Run locally

```sh
./mvnw spring-boot:run
```

On Windows, use `mvnw.cmd spring-boot:run`.
