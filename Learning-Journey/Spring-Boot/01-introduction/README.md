# Spring Boot Milestone 01: Introduction

An introductory Spring Boot project used to learn the standard Maven-based application structure.

## What I practised

- Spring Boot project conventions
- Maven configuration
- The application entry point and test structure

## Run locally

```sh
./mvnw spring-boot:run
```

On Windows, use `mvnw.cmd spring-boot:run`.
